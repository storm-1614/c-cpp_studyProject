# 计算机系统概述
## 计算机发展简史  
略
### 冯洛依曼机（提出**存储程序**的思想）  
将事先编好的程序和原始数据送入主存中，然后启动执行。计算机能在不需操作人员干预下自动完成逐条取出质量和执行指令的任务。    
ENIAC 第一台计算机。  
### 冯·洛依曼结构：
- 存储器：存放数据、存放指令  
- 控制器：自动执行指令
- 运算器：进行算术、逻辑和附加运算
- 输入设备
- 输出设备

内部以二进制表示指令和数据，每条指令由操作码和地址码两部分组成。操作码指出操作类型，地址码指出操作数的地址，由一串指令组成程序。  

采用**存储程序**的方式在工作。  

![冯·洛依曼结构示意图](1.png)

IAS 计算机结构。  
## 计算机系统的组成
### 中央处理器
运算器：ALU, GPRS,标志寄存器等  
控制器：对指令译码生成控制信号  
### 存储器
存储阵列、地址译码器
### 外部设备和设备控制器

### 总线（数据传输）
MDR、MAR、控制线。  

## 计算机层次结构

## 存储器

## 运算器
- 算术/逻辑运算、比较、转移和测试等功能
- 根据运算的结果设置条件状态寄存器

## 系统
基于（共享）总线的系统互连  

基于专用通道结构（专用通道总线）的系统互连  

冯洛依曼结构，哈佛结构。  

cache 采用哈佛结构，主存采用冯洛依曼结构。  

机器语言： 0 , 1，汇编语言：用助记符表示机器指令啥的  

*好无聊，东扯西扯的不知道在讲什么*  

## 计算机运行程序的过程

预编译、汇编、链接  

操作系统、ISA 之类

## 计算机性能评价
- 制造成本
- 衡量计算机性能指标
    * 响应时间
        * 执行时间、等待时间
    * 吞吐量
        * 带宽
        * 吞吐率


    

计算机有两种不同的性能  
* 响应时间
* 执行时间
* 等待时间或时延

基本性能评价的标准是：CPU 的执行时间。  

机器 X 的速度（性能）是Y的n倍：  
$$
\frac{ExTime(X)}{ExTime(Y)} = \frac{Performance(X)}{Performance(Y)} = n
$$

相对性能用执行时间的倒数来表示。  

比较计算机的信念时，用执行时间来衡量。  

时间指标：  
(1) 主频f(Hz)/时钟周期T(秒)  
f 与 T 互为倒数。T 也成为节拍周期，是处理操作最基本的时间单位。  
CPU 主频越高，T周期时间就越短。  
(2) CPI(Clock cycles Per Instruction)
执行一条指令（平均）需要的时钟周期数（即T周期的个数）  
用平均值。  
CPI = 程序所有指令的时钟周期数之和/程序中的指令总数  
$$
CPI = \sum(程序中的各类指令的 CPI \times 程序中该类指令的比例)
$$



> CPI 越小 CPU 速度越快吗？



IPC：CPU 每一时钟周期内所执行的指令是多少。  

MIPS(Million Instructions Per Second):  
每秒钟CPU执行的指令条数（以百万条为单位）  

$$
MIPS = \frac{指令条数}{执行时间 \times 10 ^6} = \frac{指令条数}{CPU时钟周期之和 \times 10^6 / f} = \frac{f}{CPI \times 10 ^ 6}
$$

峰值 MIPS: 选取一组指令组合，使得得到的平均 CPI 最小，由此得到的 MIPS 就是峰值 MIPS。    

MFLOPS:  
描述计算机每秒钟执行的浮点操作次数。  
世界最快的超算：2.198 ExaFLOPS    

CPU 执行时间：执行一段程序所需的时间    
(CPU时间 + I/O时间 + 存储访问时间+各类排队时延等)。  
考虑 CPU  


Amdahl 定律：  
对系统中某部分（硬件或软件）进行更新所带来的系统性能改进程度，取决于该部分被使用的频率或其执行时间占总执行时间的比例。  
$$
改进后执行时间=\frac{改进部分执行时间}{改进部分的改进倍数}+未改进部分执行时间  
$$

或  
$$
整体改进倍数=\frac{1}{改进部分时间比例/改进部分的改进倍数+未改进部分时间比例}
$$

$$
p = \frac{1}{\frac{t}{n}+1-t}
$$

